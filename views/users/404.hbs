{{>header}}

<section class="mx-auto w-full max-w-2xl">
  <div class="card card-lg text-center">
    <h1 class="text-2xl font-semibold">Page Not Found</h1>
    <p class="mt-2 text-sm text-slate-500 dark:text-slate-400">The link is invalid or has expired.</p>
    <a href="/login" class="mt-5 inline-flex items-center justify-center rounded-lg bg-primary-600 px-4 py-2 text-sm font-semibold text-white hover:bg-primary-700">Go to Login</a>
  </div>
</section>

{{>footer}}